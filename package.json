{
    "engines": {
        "node": ">=20.16.0"
    },
    "type": "module",
    "scripts": {
        "docs:dev": "vitepress dev",
        "docs:build": "vitepress build",
        "docs:preview": "vitepress preview",
        "docs:start": "npm run docs:build && npm run docs:dev"
    },
    "devDependencies": {
        "vitepress": "^1.3.4"
    }
}